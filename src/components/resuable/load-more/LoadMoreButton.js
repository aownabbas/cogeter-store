import React from "react";

const LoadMoreSpiner = ({ loading, onClick }) => {
  return (
    <div className="loadmore_button__container">
      <div className="loading-spinner"></div>
    </div>
  );
};

export default LoadMoreSpiner;
